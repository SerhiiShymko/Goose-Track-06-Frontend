import { NavLink } from "react-router-dom";

const NotFoundPage = () => {
  return (
    <div>
      NotFoundPage
      <NavLink to="/calendar">Calendar</NavLink>
    </div>
    
  );
};

export default NotFoundPage;
